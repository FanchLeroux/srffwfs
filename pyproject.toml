[project]
name = "srffwfs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fleroux", email = "francois.leroux.pro@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
]

[project.scripts]
srffwfs = "srffwfs:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
